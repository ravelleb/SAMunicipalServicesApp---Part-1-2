<Window x:Class="SAMunicipalServicesApp.ServiceRequestStatusWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Service Request Status" Height="450" Width="600">
    <Grid Background="#f3f4f6">
        <StackPanel Margin="20,20,20,50">
            <!--Service Request Status -->
            <TextBlock Text="Service Request Status (Coming Soon)" 
                       FontSize="18" 
                       FontWeight="Bold" 
                       Foreground="#005B9A" 
                       HorizontalAlignment="Center" 
                       VerticalAlignment="Center"
                       Margin="0,0,0,20"/>
        </StackPanel>

       
        <Button Content="Back to Main Menu" 
                Click="btnBack_Click" 
                Height="30" 
                Width="150"
                Background="#005B9A" 
                Foreground="White"
                FontWeight="Bold"
                HorizontalAlignment="Center"
                VerticalAlignment="Bottom"
                Margin="0,0,0,20"
                BorderBrush="Transparent"
                BorderThickness="0"
                Padding="10"/>
    </Grid>
</Window>
